# 2026-05-25 学習ログ

## 今日学習したこと

今日は、paiza形式の標準入力と、文字列・数値の扱いについて学習した。

---

## 1. 2行入力の受け取り方

最初に、文字列 `s` と `t` が2行で与えられる問題に取り組んだ。

入力形式:

```text
s
t
```

この場合は、入力全体を改行ごとに分ける。

```js
const input = fs.readFileSync(0, "utf-8").trim().split("\n");

const s = input[0];
const t = input[1];
```

`split("\n")` は、文字列を改行で分けて配列にする処理。

---

## 2. 1行スペース区切りの受け取り方

次に、文字列 `s_1` と `s_2` が1行で半角スペース区切りで与えられる問題に取り組んだ。

入力形式:

```text
s_1 s_2
```

この場合は、改行ではなく半角スペースで分ける。

```js
const input = fs.readFileSync(0, "utf-8").trim().split(" ");

const s_1 = input[0];
const s_2 = input[1];
```

`split(" ")` は、文字列を半角スペースで分けて配列にする処理。

---

## 3. `split("\\n")` と `split(" ")` の違い

今日かなり大事だったポイント。

- `split("\n")` は改行で分ける
- `split(" ")` は半角スペースで分ける

例1（改行区切り）:

```text
apple
banana
```

結果:

```js
["apple", "banana"];
```

例2（スペース区切り）:

```text
apple banana
```

結果:

```js
["apple", "banana"];
```

つまり、見るべきポイントは「入力が何で区切られているか」。

- `s` / `t` のように行が分かれているなら `split("\n")`
- `s_1 s_2` のように1行なら `split(" ")`

---

## 4. `const` と `split()` は別の話

途中で、`const` の定数と `input` の中身について整理した。

```js
const input = ...;
```

これは「`input` という変数に別の値を再代入できない」という意味。

一方で、今回の問題で重要だったのは `const` ではなく、`split()` の区切り方。

```js
const input = fs.readFileSync(0, "utf-8").trim().split("\n");
```

が正しいか、

```js
const input = fs.readFileSync(0, "utf-8").trim().split(" ");
```

が正しいかは、入力形式によって決まる。

---

## 5. 差と積を出力する問題

最後に、整数 `A`, `B` が半角スペース区切りで与えられ、差 `D` と積 `P` を出力する問題に取り組んだ。

入力形式:

```text
A B
```

1行の中に半角スペース区切りで2つなので、`split(" ")` を使う。

最初に書いたコード:

```js
const fs = require("fs");
const input = fs.readFileSync(0, "utf-8").trim().split(" ");

let a = input[0];
let b = input[1];

const d = a - b;
const p = a * b;

console.log(d, p);
```

このコードは動く。

理由は、JavaScript では `-` や `*` を使うと、文字列の数字を自動で数値として扱ってくれるから。

```js
"8" - "3"; // 5
"8" * "3"; // 24
```

---

## 6. より安全な書き方

ただし、競プロや paiza では、数字として使う入力は最初に `Number()` で変換する方が安全。

```js
const a = Number(input[0]);
const b = Number(input[1]);
```

より安全な完成形:

```js
const fs = require("fs");
const input = fs.readFileSync(0, "utf-8").trim().split(" ");

const a = Number(input[0]);
const b = Number(input[1]);

const d = a - b;
const p = a * b;

console.log(d, p);
```

---

## 今日の重要ポイント

- `split("\n")` は改行区切り
- `split(" ")` は半角スペース区切り
- 入力形式を見て、どちらの `split()` を使うか判断する
- 数値計算をするときは、最初に `Number()` で文字列を数値に変換すると安全

```js
const a = Number(input[0]);
const b = Number(input[1]);
```
