# 2026年8月4日 アルゴリズム学習ログ

## 今日の学習内容

Paizaの問題を使い、コードを書く前に問題を分解する練習をした。

- 入力例を手作業で計算
- 掛け算と繰り返し処理の関係を確認
- `if`文による勝敗判定
- `Math.floor()`による小数点以下の切り捨て
- 前回の計算結果を次の処理へ引き継ぐ方法
- Git BashからNode.jsへ入力を渡して動作確認
- 実行時エラーの原因を確認して修正

---

## 1日Nページを100日続ける問題

### 問題

1日に`N`ページ進める場合、100日後に何ページ終わるか求める。

入力例：

```text
50
```

手作業で計算する。

```text
1日：50ページ
日数：100日

50 × 100 = 5000
```

コード：

```js
const fs = require("fs");
const input = fs.readFileSync(0, "utf-8").trim().split(/\s+/);

const n = Number(input[0]);
const page = n * 100;

console.log(page);
```

出力：

```text
5000
```

`page`は後から変更しないため、`let`ではなく`const`を使用できる。

---

## 1ダース12本の鉛筆問題

### 問題

購入したダース数`N`が与えられるので、鉛筆の合計本数を求める。

入力例：

```text
3
```

手作業で考える。

```text
1ダース目：12本
2ダース目：12本
3ダース目：12本

12 + 12 + 12 = 36
```

掛け算では次のようになる。

```text
3ダース × 12本 = 36本
```

コード：

```js
const fs = require("fs");
const input = fs.readFileSync(0, "utf-8").trim().split(/\s+/);

const n = Number(input[0]);
const total = n * 12;

console.log(total);
```

### for文で考える場合

`12`をダース数`n`回足す。

```js
let total = 0;

for (let i = 0; i < n; i++) {
  total += 12;
}
```

入力が`3`なら次のように変化する。

```text
開始時：0
1回目：0 + 12 = 12
2回目：12 + 12 = 24
3回目：24 + 12 = 36
```

次のコードでは、`0`を足し続けているため結果も`0`になる。

```js
let total = 0;

for (let i = 0; i < 12; i++) {
  total += 0;
}
```

この問題は掛け算で求められるため、`n * 12`が簡潔である。

---

## 勇者のレベル変化問題

### ルール

- 自分のレベルが高ければ勝利
- 勝利すると、相手のレベルの半分が自分のレベルに加算される
- 自分のレベルが低ければ敗北
- 敗北すると、自分のレベルが半分になる
- 同じレベルなら引き分け
- 半分にした結果は小数点以下を切り捨てる

入力例：

```text
5 10
5
11
20
8
7
```

最初の行：

```text
N = 5
最初の自分のレベル = 10
```

---

## 入力例を手作業で追う

1人目はレベル`5`。

```text
10 > 5なので勝利
Math.floor(5 ÷ 2) = 2
10 + 2 = 12
```

2人目はレベル`11`。

```text
12 > 11なので勝利
Math.floor(11 ÷ 2) = 5
12 + 5 = 17
```

3人目はレベル`20`。

```text
17 < 20なので敗北
Math.floor(17 ÷ 2) = 8
```

4人目はレベル`8`。

```text
8 === 8なので引き分け
レベルは8のまま
```

5人目はレベル`7`。

```text
8 > 7なので勝利
Math.floor(7 ÷ 2) = 3
8 + 3 = 11
```

レベルの変化：

```text
10 → 12 → 17 → 8 → 8 → 11
```

出力：

```text
11
```

---

## 条件をif文へ変換

変数名：

```text
現在の自分のレベル → currentLevel
相手のレベル       → opponentLevel
```

`opponent`のスペルに注意する。

```js
if (currentLevel > opponentLevel) {
  // 勝利
} else if (currentLevel < opponentLevel) {
  // 敗北
} else {
  // 引き分け
}
```

### 勝利した場合

```js
currentLevel += Math.floor(opponentLevel / 2);
```

相手がレベル`5`の場合：

```text
5 ÷ 2 = 2.5
Math.floor(2.5) = 2
```

### 敗北した場合

```js
currentLevel = Math.floor(currentLevel / 2);
```

自分がレベル`17`の場合：

```text
17 ÷ 2 = 8.5
Math.floor(8.5) = 8
```

### 引き分けの場合

レベルを変更しないため、更新処理は不要。

---

## 完成したコード

```js
const fs = require("fs");
const input = fs.readFileSync(0, "utf-8").trim().split(/\s+/);

const n = Number(input[0]);
let currentLevel = Number(input[1]);

for (let i = 0; i < n; i++) {
  const opponentLevel = Number(input[i + 2]);

  if (currentLevel > opponentLevel) {
    currentLevel += Math.floor(opponentLevel / 2);
  } else if (currentLevel < opponentLevel) {
    currentLevel = Math.floor(currentLevel / 2);
  }
}

console.log(currentLevel);
```

---

## input[i + 2]の理由

入力を分割すると次の配列になる。

```js
[
  "5",
  "10",
  "5",
  "11",
  "20",
  "8",
  "7",
]
```

対応関係：

```text
input[0] → 戦闘回数N
input[1] → 最初の自分のレベル
input[2] → 1人目の相手
input[3] → 2人目の相手
input[4] → 3人目の相手
```

`i`は`0`から始まるので、相手のレベルは次で取得できる。

```js
input[i + 2]
```

---

## ターミナルでの動作確認

Git Bashでは、`node`が次のエイリアスになっていた。

```text
node is aliased to `winpty node.exe'
```

パイプで入力すると、次のエラーが発生した。

```text
stdin is not a tty
```

パイプ入力では`node.exe`を直接使用する。

```bash
echo "5 10 5 11 20 8 7" | node.exe battle.js
```

実行結果：

```text
11
```

入力読み込みでは、スペースと改行の両方を区切りとして扱っている。

```js
const input = fs.readFileSync(0, "utf-8").trim().split(/\s+/);
```

そのため、問題文の入力を1行にして渡しても動作する。

---

## 発生したエラー

### MODULE_NOT_FOUND

```text
Error: Cannot find module '...\main.js'
```

指定したファイルが現在のフォルダに存在していなかった。

実際のファイル名を確認して実行する。

```bash
echo "入力" | node.exe battle.js
```

### ReferenceError: 問題 is not defined

```js
問題;
```

JavaScriptは`問題`を変数として解釈する。

日本語のメモにはコメントを付ける。

```js
// 問題
```

### ReferenceError: input is not defined

入力を読み込むコードが不足していた。

```js
const fs = require("fs");
const input = fs.readFileSync(0, "utf-8").trim().split(/\s+/);
```

過去の問題を1つのファイルに書き続けると、以前のコードも一緒に実行される。

今後は問題ごとにファイルを分ける。

```text
page.js
dozen.js
battle.js
```

---

## 今日理解したこと

- コードを書く前に入力例を手作業で追う
- 1回の処理で値がどう変化するか確認する
- 更新後の値を次の処理へ引き継ぐ
- 条件が複数ある場合は`if / else if / else`を使う
- `Math.floor()`で小数点以下を切り捨てられる
- 同じ数を繰り返し足す処理は掛け算にできる
- `for`文では、何を何回繰り返すかを具体例から考える
- JavaScript内の日本語メモには`//`を付ける
- 問題ごとにファイルを分ける
- Git Bashのパイプ入力では`node.exe`を使用する
- エラーでは、ファイル名、行番号、エラーの種類を確認する

---

## 次回やること

次回はPaizaの次の問題から再開する。

1. 入力と出力を整理する
2. 入力例を手作業で解く
3. 必要な変数を決める
4. 条件分岐や繰り返しが必要か考える
5. まず自分でコードを書く
6. `node.exe`を使って動作確認する
7. 分からない場合は最初のヒントから確認する