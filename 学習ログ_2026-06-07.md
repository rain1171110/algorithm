# 2026-06-07 学習ログ

## 今日のテーマ

**JavaScript 標準入力と条件分岐・for文の復習**

今日は paiza 形式の問題を使って、標準入力の受け取り方、数値変換、条件分岐、そして `for` 文で連続した数字を出力する練習をした。

---

## 学習内容

### 1. 標準入力の受け取り方

Node.js では、標準入力を受け取るために次の形を使う。

```js
const fs = require("fs");
const input = fs.readFileSync(0, "utf-8").trim().split("\n");
```

ポイントは、改行で分けるときは、

```js
split("\n");
```

を使うこと。

間違えて、

```js
split("/n");
```

と書くと、改行として認識されない。

---

### 2. 文字列を数値に変換する

標準入力で受け取った値は、最初は文字列なので、数値として使う場合は `Number()` を使う。

```js
const n = Number(input[0]);
```

これで、入力された値を数値として扱える。

---

### 3. 条件分岐の復習

占いの問題では、入力された数字が `7` なら `"Yes"`、それ以外なら `"No"` を出力した。

```js
if (n === 7) {
  console.log("Yes");
} else {
  console.log("No");
}
```

注意点として、出力は問題文と完全に一致させる必要がある。

```js
"No";
```

と

```js
"NO";
```

は別物なので、paiza では不正解になる。

---

### 4. 変数名の大文字・小文字

JavaScript では、`n` と `N` は別の変数として扱われる。

```js
const n = Number(input[0]);
console.log(N);
```

このように書くと、`N` という変数は存在しないためエラーになる。

正しくは、

```js
console.log(n);
```

---

### 5. for文で 1 から N まで出力する

「1 ~ N の整数を 1 から順に改行区切りで出力する」問題では、`for` 文を使う。

```js
for (let i = 1; i <= n; i++) {
  console.log(i);
}
```

意味は、

```text
i を 1 から始める
i が n 以下の間くり返す
1回終わるごとに i を 1 増やす
```

例えば `n = 5` の場合、

```text
1
2
3
4
5
```

と出力される。

---

## 今日の修正版コード

```js
const fs = require("fs");
const input = fs.readFileSync(0, "utf-8").trim().split("\n");

const n = Number(input[0]);

for (let i = 1; i <= n; i++) {
  console.log(i);
}
```

---

## 今日の理解ポイント

今日のポイントは、**入力を受け取る → 数値に変換する → for文で必要な回数だけ出力する** という流れ。

特に重要だったのはこの3つ。

```js
split("\n");
```

```js
const n = Number(input[0]);
```

```js
for (let i = 1; i <= n; i++) {
  console.log(i);
}
```

paiza の問題では、コードの考え方だけでなく、**出力文字の大文字小文字や改行も完全一致** させる必要がある。

---

## 今日の感想

標準入力の形には少し慣れてきた。
ただ、`\n` と `/n` の違いや、`n` と `N` の違いなど、細かいミスでエラーや不正解になることが分かった。

for文はまだ慣れが必要だけど、
「1から始めて、Nまで、1ずつ増やす」
という流れで考えると理解しやすい。

---

## 次回やること

次回は、`for` 文をもう少し練習する。

特に、

```js
for (let i = 0; i < n; i++) {
  // 0 から n-1 まで回す
}
```

と

```js
for (let i = 1; i <= n; i++) {
  // 1 から n まで回す
}
```

の違いを整理する。

ここが分かると、繰り返し問題がかなり解きやすくなる。
