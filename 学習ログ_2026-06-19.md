# 2026-06-19 学習ログ

## 今日のテーマ

**標準出力の基本と、改行区切り・半角スペース区切りの違いを理解する**

今日は paiza 系の基礎問題を通して、`console.log()` の使い方、`for` 文での連続出力、配列と `join()` を使った出力形式の調整を練習した。

---

## 学習内容

### 1. `console.log()` は自動で改行される

最初に、決まった整数をそのまま出力する問題を解いた。

```js
console.log(8);
console.log(13);
```

このように `console.log()` を複数回使うと、それぞれ改行されて出力される。

出力：

```text
8
13
```

今日の重要ポイントは、
**`console.log()` 1回につき1行出力される** ということ。

---

### 2. 改行区切りで出す場合

整数 `8, 1, 3` を改行区切りで出す問題では、最初にこう書いた。

```js
const n = (8, 1, 3);
console.log(n);
```

ただし、この書き方では `8, 1, 3` が全部保存されるわけではなく、最後の `3` だけが出力される。

正しい書き方はこれ。

```js
console.log(8);
console.log(1);
console.log(3);
```

出力：

```text
8
1
3
```

---

### 3. たくさんの数を出力する方法

10個の整数を改行区切りで出す問題では、まず `console.log()` を並べて書いた。

```js
console.log(813);
console.log(1);
console.log(2);
console.log(923874);
console.log(23648);
console.log(782356);
console.log(3256);
console.log(2342);
console.log(24324);
console.log(112);
```

これは正解。

ただし、他の書き方として配列を使う方法も確認した。

```js
const numbers = [813, 1, 2, 923874, 23648, 782356, 3256, 2342, 24324, 112];

for (const number of numbers) {
  console.log(number);
}
```

また、`join("\n")` を使えば、改行区切りでまとめて出力できる。

```js
const numbers = [813, 1, 2, 923874, 23648, 782356, 3256, 2342, 24324, 112];

console.log(numbers.join("\n"));
```

---

### 4. `for` 文で 1〜1000 を出力する

1以上1000以下の整数を昇順で出力する問題を解いた。

書いたコード：

```js
for (let i = 1; i <= 1000; i++) {
  console.log(i);
}
```

これは正解。

より問題文に近い書き方として、次の形も確認した。

```js
for (let i = 1; i < 1001; i++) {
  console.log(i);
}
```

`i <= 1000` と `i < 1001` は、どちらも同じ範囲を表せる。
**「1000以下」なら `i <= 1000` が直感的で分かりやすい。**

---

### 5. `if / else if / else` は1つだけ実行される

`if / else if / else` を使って、条件に合う1つだけを出力する形も確認した。

```js
const n = 2;

if (n === 1) {
  console.log("one");
} else if (n === 2) {
  console.log("two");
} else {
  console.log("other");
}
```

この形では、上から順に判定して、最初に当てはまったブロックだけが実行される。

---

### 6. 半角スペース区切りの出力

今日かなり大事だったのが、**改行区切りと半角スペース区切りの違い**。

たとえば、

```js
const numbers = [8, 1, 3];

for (let i = 0; i < numbers.length; i++) {
  console.log(numbers[i]);
}
```

これは出力がこうなる。

```text
8
1
3
```

つまり、改行区切り。

でも問題が求めているのは、

```text
8 1 3
```

このような半角スペース区切り。

その場合は `join(" ")` を使う。

```js
const numbers = [8, 1, 3];

console.log(numbers.join(" "));
```

出力：

```text
8 1 3
```

---

## 今日の重要ポイント

### 改行区切り

```js
console.log(8);
console.log(1);
console.log(3);
```

または、

```js
console.log([8, 1, 3].join("\n"));
```

---

### 半角スペース区切り

```js
console.log("8 1 3");
```

または、

```js
console.log(8, 1, 3);
```

または、

```js
const numbers = [8, 1, 3];

console.log(numbers.join(" "));
```

---

## 今日つまずいたところ

### `console.log()` をループの中に書くと毎回改行される

```js
for (let i = 0; i < numbers.length; i++) {
  console.log(numbers[i]);
}
```

これは半角スペース区切りではなく、改行区切りになる。

### `join(" ")` をループの中で使うと何回も出力される

```js
for (let i = 0; i < numbers.length; i++) {
  console.log(numbers.join(" "));
}
```

この場合、同じ内容が3回出てしまう。

```text
8 1 3
8 1 3
8 1 3
```

`join(" ")` は配列全体をまとめて出力するので、ループの中ではなく1回だけ使えばよい。

---

## 最終的に理解したこと

今日は、出力問題の基本をかなり整理できた。

特に、

```js
console.log();
```

は改行される。

```js
join("\n");
```

は改行区切り。

```js
join(" ");
```

は半角スペース区切り。

この3つの違いが重要。

paiza の出力問題では、答えの計算だけでなく、
**指定された出力形式に合わせること** がとても大事だと学んだ。
