# 学習ログ：標準入力と出力形式の練習

## 今日のテーマ

今日は、JavaScriptで標準入力から数値を受け取り、指定された形式で出力する練習を行った。

主に次の2つを学習した。

* 1からNまでの数値を改行区切りで出力する
* 2つの数値をカンマ区切りで出力する

---

## 1. 1からNまでを改行区切りで出力する

### 問題内容

整数 `N` が入力される。
`1` から `N` までの数値をすべて、改行区切りで出力する。

### 学習したコード

```js
const fs = require("fs");
const input = fs.readFileSync(0, "utf-8").trim().split(/\s+/);

const n = Number(input[0]);
let result = "";

for (let i = 1; i <= n; i++) {
  result += i;

  if (i < n) {
    result += "\n";
  }
}

console.log(result);
```

### 理解したこと

`for (let i = 1; i <= n; i++)` は、`1` から入力された `n` まで繰り返すという意味。

最初は `i <= 5` と書きそうになったが、条件の `1 ≦ N ≦ 5` は「Nの最大値は5」という意味であり、「必ず5まで出力する」という意味ではないと理解した。

また、`result += i;` で出力したい数字を文字列としてためていき、最後以外のときだけ `result += "\n";` で改行を追加する。

最後に `console.log(result);` で、ためた文字列をまとめて出力する。

---

## 2. 2つの数値をカンマ区切りで出力する

### 問題内容

2つの数値が半角スペース区切りで与えられる。
これらの数値をカンマ区切りで出力する。

### 学習したコード

```js
const fs = require("fs");
const input = fs.readFileSync(0, "utf-8").trim().split(/\s+/);

const n = Number(input[0]);
const m = Number(input[1]);

console.log(n + "," + m);
```

### 理解したこと

`input[0]` で1つ目の値、`input[1]` で2つ目の値を取得できる。

```js
const n = Number(input[0]);
const m = Number(input[1]);
```

このように書くことで、入力された文字列を数値として扱える。

最初は次のように書いた。

```js
console.log(n, ",", m);
```

しかし、`console.log()` にカンマで複数の値を渡すと、出力時に余計な半角スペースが入ることがある。

そのため、今回のようにスペースなしで `3,5` のように出力したい場合は、次のように文字列として結合する。

```js
console.log(n + "," + m);
```

これにより、余計なスペースを入れずにカンマ区切りで出力できる。

---

## 今日の重要ポイント

### 標準入力の基本形

```js
const fs = require("fs");
const input = fs.readFileSync(0, "utf-8").trim().split(/\s+/);
```

この形で、入力された値を配列として受け取ることができる。

---

### 数値への変換

```js
const n = Number(input[0]);
```

`input` に入っている値は最初は文字列なので、計算や数値として扱う場合は `Number()` で変換する。

---

### 出力形式に注意する

```js
console.log(n + "," + m);
```

出力問題では、答えの内容だけでなく、スペースや改行の位置も大事。

`console.log(n, ",", m)` のように書くと、意図しないスペースが入ることがあるため、指定された形式に合わせて文字列結合を使う。

---

## 今日のまとめ

今日は、標準入力から値を受け取り、指定された形式で出力する練習をした。

特に、`1 ≦ N ≦ 5` のような条件は「Nの範囲」を表しているだけであり、必ず5まで出力するという意味ではないことを理解した。

また、出力形式では、改行区切りやカンマ区切りなど、問題文で指定された形に正確に合わせる必要があると学んだ。

---

## 次回やること

次回はこの続きから実施する。

特に、次の内容を復習する。

* 標準入力の受け取り方
* `input[0]`, `input[1]` の使い方
* `Number()` による数値変換
* `for` 文で繰り返す処理
* `result` に文字列をためて出力する処理
* 改行区切り、半角スペース区切り、カンマ区切りの違い
